'''
from scipy.optimize import root
from math import cos
def eqn(x):
  return x + cos(x)    # 返回非线性函数的根
myroot = root(eqn, 0)
print(myroot.x)
# 查看更多信息
#print(myroot)
'''
'''
from scipy.optimize import minimize
def eqn(x):
  return x**2 + x + 2
mymin = minimize(eqn, 0, method='BFGS')   # 最小化函数， 在给定约束或无约束条件下，寻找目标函数最小值及对应输入值的数学工具
print(mymin)
'''
'''
最小化函数报告
字段名 通俗含义  专业解读
message 优化结果提示：“优化成功终止” 算法正常收敛，未出现异常（如超时、梯度爆炸等）
success 是否成功：True（成功） 布尔值标记，True 表示找到有效最优解，False 表示优化失败（需查原因）
status  状态码：0（成功） 状态码规则：0= 成功收敛；1= 达到最大迭代次数；2= 目标函数值变化小于阈值；3= 无效输入等
fun 目标函数的最小值：1.75 当参数取 x 时，目标函数 f(x) 的输出（你要最小化的指标最终结果）
x 对应最小值的最优参数：[-0.5]（即 x = -0.5） 让 f(x) 达到最小值 1.75 的输入变量（若多参数，会是一个数组，如 [x1, x2]）
nit 迭代次数：2  算法调整参数的次数（次数越少，说明函数简单或算法高效）
jac 最优解处的梯度值：[0.0]  目标函数在 x=-0.5 处的导数（梯度）为 0，符合 “极值点” 的数学条件（导数为 0 是最小值 / 最大值的必要条件）
hess_inv  海森矩阵的逆矩阵：[[0.5]]  海森矩阵描述函数的曲率，逆矩阵用于梯度下降类算法的步长调整（此处是 1x1 矩阵，因只有 1 个参数）
nfev  目标函数调用次数：8  算法过程中计算 f(x) 的次数（反映算法效率，次数越少越优）
njev  梯度函数调用次数：4  算法过程中计算梯度（导数）的次数（梯度计算耗时，次数少更高效）
'''


