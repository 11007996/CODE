<Window x:Class="WPFCode.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="MainWindow" Height="350" Width="350">
    <!--<WindowChrome.WindowChrome>
        <WindowChrome UseAeroCaptionButtons="False"></WindowChrome>
    </WindowChrome.WindowChrome>-->

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="mySource.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="txtt" Grid.Row="0" Grid.Column="1" Text="{Binding Name,TargetNullValue={StaticResource ResourceKey=str}, Mode=TwoWay}" FontSize="20"></TextBlock>
        <Button x:Name="btnModify" Grid.Row="1" Grid.Column="1" Content="修改" Height="30" Width="50" Click="ModifyText"></Button>
        <Button x:Name="btnText" Grid.Row="1" Grid.Column="0" Content="读取" Height="30" Width="50" Click="readText"></Button>
        <Button x:Name="btnClose" Grid.Row="2" Grid.Column="0" Content="关闭" Height="30" Width="50" Click="winClose"></Button>
        <Button x:Name="btnStyle" Style="{StaticResource safe}" Grid.Row="0" Grid.Column="2" Content="点击" BorderBrush="Beige" BorderThickness="2"></Button>
        <Button>
            
        </Button>
        <Rectangle Grid.Row ="2" Grid.Column="2" Width="auto" Height="auto" StrokeThickness="5">
                <Rectangle.Fill>
                <!--<LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="AliceBlue" Offset="0"></GradientStop>
                    <GradientStop Color="Black" Offset="0.7"></GradientStop>
                    <GradientStop Color="Beige" Offset="1"></GradientStop>
                </LinearGradientBrush>-->
                <ImageBrush ImageSource="{Binding Source={StaticResource ResourceKey=imgPath}}" Stretch="Fill"></ImageBrush>

                </Rectangle.Fill>


            <Rectangle.Stroke>
                <!--线性渐变-->
                <!--<LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="Red" Offset="0"></GradientStop>
                    <GradientStop Color="Black" Offset="0.7"></GradientStop>
                    <GradientStop Color="Blue" Offset="1"></GradientStop>
                </LinearGradientBrush>-->
                
                <!--径向渐变-->
                <!--<RadialGradientBrush Center="0,0.5"  GradientOrigin="0.3,0.3" RadiusX="0.5" RadiusY="0.5" SpreadMethod="Repeat">
                    <GradientStop Color="Red" Offset="0"></GradientStop>
                    <GradientStop Color="Black" Offset="0.5"></GradientStop>
                    <GradientStop Color="Blue" Offset="1"></GradientStop>
                </RadialGradientBrush>-->
                
                <!--实体填充-->
                <SolidColorBrush Color="Cyan"></SolidColorBrush>
            </Rectangle.Stroke>
        </Rectangle>
    </Grid>
</Window>
