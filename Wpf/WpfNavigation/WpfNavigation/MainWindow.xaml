<Window x:Class="WpfNavigation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfNavigation.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="500">
    <Window.Resources>
        <vm:pageAViewModel x:Key="pageA"/>
        <vm:pageBViewModel  x:Key="pageB"/>
        <vm:pageCViewModel  x:Key="pageC"/>
        <Style x:Key="radioS" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton" x:Name="radioTemp">
                        <Border Height="80" Width="120">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <RadialGradientBrush GradientOrigin="0.5,0.55" Center="0.5,0.55" RadiusX="0.6" RadiusY="0.5">
                                                        <GradientStop Color="DarkGreen" Offset="0"/>
                                                        <GradientStop Color="Cornsilk" Offset="0.7"/>
                                                        <GradientStop Color="Transparent" Offset="1"/>
                                                    </RadialGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"></ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <ContentControl x:Name="pageControl" Content="{Binding UseView,TargetNullValue={StaticResource pageA}}"></ContentControl>
        <UniformGrid Grid.Row="1" Columns="3" >
            <RadioButton x:Name="radioA"  Style="{StaticResource radioS}" Command="{Binding radioCommand}" CommandParameter="{StaticResource pageA}" Content="pageA" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <RadioButton x:Name="radioB" Style="{StaticResource radioS}" Command="{Binding radioCommand}" CommandParameter="{StaticResource pageB}" Content="pageB"  FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <RadioButton x:Name="radioC" Style="{StaticResource radioS}" Command="{Binding radioCommand}" CommandParameter="{StaticResource pageC}" Content="pageC"  FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </UniformGrid>
    </Grid>
</Window>
