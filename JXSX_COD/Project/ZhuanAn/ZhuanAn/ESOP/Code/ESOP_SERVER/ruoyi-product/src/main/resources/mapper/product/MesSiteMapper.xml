<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.product.mapper.MesSiteMapper">

    <resultMap type="com.ruoyi.product.domain.MesSite" id="MesSiteResult">
        <result property="siteId"    column="site_id"    />
        <result property="siteName"    column="site_name"    />
        <result property="status"    column="status"    />
        <result property="remark"    column="remark"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectMesSiteVo">
        select site_id, site_name, status, remark, create_by, create_time, update_by, update_time from mes_site
    </sql>

    <select id="selectMesSiteList" parameterType="MesSite" resultMap="MesSiteResult">
        <include refid="selectMesSiteVo"/>
        <where>
            <if test="siteName != null  and siteName != ''"> and site_name like concat('%', #{siteName}, '%')</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>

    <select id="selectMesSiteBySiteId" parameterType="Long" resultMap="MesSiteResult">
        <include refid="selectMesSiteVo"/>
        where site_id = #{siteId}
    </select>

    <select id="checkSiteNameUnique" parameterType="String" resultMap="MesSiteResult">
        <include refid="selectMesSiteVo"/>
        where site_name=#{siteName} limit 1
    </select>

    <insert id="insertMesSite" parameterType="MesSite" useGeneratedKeys="true" keyProperty="siteId">
        insert into mes_site
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="siteName != null and siteName != ''">site_name,</if>
            <if test="status != null">status,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="siteName != null and siteName != ''">#{siteName},</if>
            <if test="status != null">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateMesSite" parameterType="MesSite">
        update mes_site
        <trim prefix="SET" suffixOverrides=",">
            <if test="siteName != null and siteName != ''">site_name = #{siteName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where site_id = #{siteId}
    </update>

    <delete id="deleteMesSiteBySiteId" parameterType="Long">
        delete from mes_site where site_id = #{siteId}
    </delete>

    <delete id="deleteMesSiteBySiteIds" parameterType="String">
        delete from mes_site where site_id in
        <foreach item="siteId" collection="array" open="(" separator="," close=")">
            #{siteId}
        </foreach>
    </delete>
</mapper>
